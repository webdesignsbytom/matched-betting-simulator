export default function randomBookieQualifyingOddsGenerator(numBetType, numBetCompetitors, sportType) {
    if (sportType === 'footballQualifying') {
        for (let i = 0; i < numBetCompetitors; i++) {
            console.log();
            for (let j = 0; j < numBetType; j++) {

            }
        }
    }
}